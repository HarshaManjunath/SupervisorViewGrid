const styles = (theme) => ({
  button: {
    margin: theme.spacing(1),
    borderRadius: 50,
    backgroundColor: "white",
    paddingLeft: "12px",
    paddingRight: "12px",
    fontSize: "13px",
    textTransform: "none",
  },
});

export default styles;
